function greeter(people) {
  // Code the function here.
  return people.forEach(people => console.log(`Hello ${people}!`));
}
// Do not remove the following lines, it is for tests
module.exports = greeter;
