import { getRandomUser } from "./index";

test("");

// mock pour vérifier chaque index d'un tableau
// jest spyOn
// mockImplementation
// appel function (...)
// toHaveBeenCalled + ...With
// mock.restore
// jouer avec .length de mon array dans spy.mockImplementation(() => ?`.
